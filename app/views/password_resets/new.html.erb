<%= form_for(:password_reset, url: password_resets_path) do |f| %>
  <div class="ui center aligned grid">
    <div class="fourteen wide column">
      <div id="please-password-reset">パスワードを更新する</div>
      <div class="depend-on-device message ui center aligned grid">
        <div class="left aligned fourteen wide column">
          <p id="procedure-of-password-reset">手順</p>
          <div class="step-of-password-reset">
            <p>step1.</p>
            <p>まずは、以下のフォームにこのアプリに登録したメールアドレスを入力し、送信してください。</p>
          </div>
          <div class="step-of-password-reset">
            <p>step2.</p>
            <p>そのメールアドレス宛にパスワードを更新するためのメールが届きます。</p>
          </div>
          <div class="step-of-password-reset">
            <p>step3.</p>
            <p>届いたメール内のURLをクリックすると、パスワードを更新するためのページが開かれます。</p>
          </div>
          <div class="step-of-password-reset">
            <p>step4.</p>
            <p>そのページ内のフォームに新しいパスワードを入力し送信すると、そのパスワードがあなたの新しいパスワードになります。</p>
          </div>
        </div>
      </div>
      <div class="depend-on-device ui form">
        <div class="field">
          <div class="ui left aligned grid">
            <div class="signup-page-frame-of-email-label sixteen wide column">
              <%= f.label :email, 'メールアドレス' %>
            </div>
            <div class="signup-page-frame-of-email-input sixteen wide column">
              <div class="ui left icon huge input">
                <%= f.email_field :email, :placeholder => "例：case@example.com" %>
                <i class="mail icon"></i>
              </div>
            </div>
          </div>
        </div>
        <div class="field">
          <div class="ui center aligned grid">
            <%= button_tag :type => "submit", :class =>"ui blue submit button ten wide column", :id => "password-reset-page-frame-of-submit" do %>
              <i class="mail icon"></i>
              送信する
            <% end %>
          </div>
        </div>
      </div>
    </div>
  </div>
<% end %>
